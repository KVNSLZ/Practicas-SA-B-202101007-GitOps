apiVersion: v1
kind: Service

metadata:
  name: servicio-pagos
  namespace: {{ .Values.global.namespace }}

spec:
  type: {{ .Values.service.type }}

  selector:
    app: servicio-pagos

  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
